---
# Kafka with KRaft dependency (post-migration) for Region 2 (East)
# Apply after KRaftMigrationJob finalization to switch from ZooKeeper to KRaft
# Security: TLS (secretRef) + SASL/PLAIN + RBAC with OAuth MDS (Keycloak)
apiVersion: platform.confluent.io/v1beta1
kind: Kafka
metadata:
  name: kafka
  namespace: east
  annotations:
    platform.confluent.io/use-log4j1: "true"
    platform.confluent.io/use-old-jetty9: "true"
    platform.confluent.io/broker-id-offset: "10"
    platform.confluent.io/kraft-migration-ibp-version: "3.9"
spec:
  replicas: 2
  dataVolumeCapacity: 10Gi

  image:
    application: confluentinc/cp-server:7.9.6
    init: confluentinc/confluent-init-container:3.2.0

  tls:
    secretRef: tls-kafka

  authorization:
    type: rbac
    superUsers:
      - User:kafka
      - User:ssologin

  services:
    mds:
      tls:
        enabled: true
      tokenKeyPair:
        secretRef: mds-token
      externalAccess:
        type: loadBalancer
        loadBalancer:
          domain: my-domain.example.com
          prefix: kafka-east-mds
          advertisedURL:
            enabled: true
            prefix: kafka-east-mds
      provider:
        oauth:
          configurations:
            expectedIssuer: http://keycloak.my-domain.example.com:8080/realms/sso_test
            jwksEndpointUri: http://keycloak.my-domain.example.com:8080/realms/sso_test/protocol/openid-connect/certs
            subClaimName: client_id
    kafkaRest:
      authentication:
        type: oauth
        oauth:
          secretRef: oauth-jass
          configuration:
            tokenEndpointUri: http://keycloak.my-domain.example.com:8080/realms/sso_test/protocol/openid-connect/token
            expectedIssuer: http://keycloak.my-domain.example.com:8080/realms/sso_test
            jwksEndpointUri: http://keycloak.my-domain.example.com:8080/realms/sso_test/protocol/openid-connect/certs
            subClaimName: client_id

  listeners:
    internal:
      tls:
        enabled: true
      authentication:
        type: plain
        jaasConfig:
          secretRef: credential
    external:
      tls:
        enabled: true
      authentication:
        type: plain
        jaasConfig:
          secretRef: credential
      externalAccess:
        type: loadBalancer
        loadBalancer:
          domain: my-domain.example.com
          brokerPrefix: kafka-east-ext
          bootstrapPrefix: kafka-east-ext
    replication:
      tls:
        enabled: true
      authentication:
        type: plain
        jaasConfig:
          secretRef: credential
      externalAccess:
        type: loadBalancer
        loadBalancer:
          domain: my-domain.example.com
          brokerPrefix: kafka-east-rep
          bootstrapPrefix: kafka-east-rep

  # KRaft dependency (post-migration) — replaces ZooKeeper dependency
  dependencies:
    kRaftController:
      clusterRef:
        name: kraftcontroller
        namespace: east
      controllerListener:
        tls:
          enabled: true
        authentication:
          type: plain
          jaasConfig:
            secretRef: credential
    kafkaRest:
      authentication:
        type: oauth
        jaasConfig:
          secretRef: oauth-jass
        oauthSettings:
          tokenEndpointUri: http://keycloak.my-domain.example.com:8080/realms/sso_test/protocol/openid-connect/token
          expectedIssuer: http://keycloak.my-domain.example.com:8080/realms/sso_test
          jwksEndpointUri: http://keycloak.my-domain.example.com:8080/realms/sso_test/protocol/openid-connect/certs
          subClaimName: client_id

  metricReporter:
    enabled: true
    bootstrapEndpoint: kafka-east-rep.my-domain.example.com:9072
    authentication:
      type: plain
      jaasConfig:
        secretRef: credential
    tls:
      enabled: true


  # CP 7.9.6: configOverrides.jvm replaces allowed.urls=* from CFK template
  configOverrides:
    jvm:
      - "-Dorg.apache.kafka.sasl.oauthbearer.allowed.urls=http://keycloak.my-domain.example.com:8080/realms/sso_test/protocol/openid-connect/certs,http://keycloak.my-domain.example.com:8080/realms/sso_test/protocol/openid-connect/token,http://keycloak.my-domain.example.com:8080/"
